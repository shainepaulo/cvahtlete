import { NoaCvExperience } from '@/components/noa/shared/NoaCvExperience'

/** Route racine : charge directement le CV de Noa avec le switch de mode. */
export default function HomePage() {
  return <NoaCvExperience />
}
